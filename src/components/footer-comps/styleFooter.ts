import styled from 'styled-components';

const StyledFooter = styled.footer({
  marginTop: 'auto',
  height: '5rem',
  width: '100%',
  color: '#777',
  borderTop: '.15rem solid #999',
  background: '#e9e9e9',

  h3: {
    height: 'inherit',
    lineHeight: '5rem',
    textAlign: 'center',
  }

});

export default StyledFooter;