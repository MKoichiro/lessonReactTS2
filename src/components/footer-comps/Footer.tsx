/* react関連の読み込み */
import React from 'react';
import ReactDOM from 'react-dom';
import StyledFooter from './styleFooter'

const Footer = () => {
  return (
    <StyledFooter>
      <h3> React x TypeScript 入門学習: To Do ツール 編 </h3>
    </StyledFooter>
  );
}

export default Footer;
